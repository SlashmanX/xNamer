var tvdb = new (require('../lib/tvrage'))('')

console.log(tvdb)

tvdb.search('"The Simpsons"', function(err, results) {
	console.log(results.series[0].id)
	tvdb.getShow(results.series[0].id, function(err, results) {
		console.log(results)
	})
})